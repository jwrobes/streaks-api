require 'rails_helper'

describe "OpenStreaks", type: :request do
  describe "GET /open_streaks" do
    xit "works! (now write some real specs)" do
      get open_streaks_path
      expect(response).to have_http_status(200)
    end
  end
end
